
<% if favorite?(post.id) %>
    お気に入り：<%= link_to "★", post_destroy_favorite_path(post), method: :DELETE, remote: true %>
<% else %>
    お気に入り：<%= link_to "☆", post_create_favorite_path(post), method: :POST, remote: true %>
<% end %>
<%= post.favorites.count%>
