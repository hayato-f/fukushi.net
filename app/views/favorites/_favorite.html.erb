
<% if favorite?(post.id) %>
    ★<%= link_to "いいねを外す", post_destroy_favorite_path(post), method: :DELETE, remote: true, class: "fas fa-bookmark" %>
<% else %>
    ☆<%= link_to "いいねする", post_create_favorite_path(post), method: :POST, remote: true, class: "fas fa-bookmark" %>
<% end %>
<%= post.favorites.count%> good
