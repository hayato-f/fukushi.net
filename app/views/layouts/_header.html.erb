<header>
    <nav class="navbar navbar-expand-lg">
        <div class="container-fluid">
            <div class="navbar-brand"><%= link_to "福祉.net", root_path %></div>
            <ul class="navbar navbar-right">
                <li class="navbar-text"><%= link_to "使い方", about_path %></li>
                <%# <li class="navbar-text">= link_to "ユーザー（一覧）", users_path</li> %>
                <li class="navbar-text"><%=  link_to "投稿一覧", root_path %></li>
            <% if logged_in? %>
                <li class="navbar-text"><%= link_to "記事の作成", new_post_path %></li>
                <li class="navbar-text"><%= link_to "#{current_user.name}_page", user_path(current_user) %></li>
                <li class="navbar-text"><%= @current_user.name %>でログイン中</li>
                <li class="navbar-text"><%= link_to "ログアウト", logout_path, method: :delete %></li>
            <% else %>
                <li class="navbar-text"><%= link_to "新規登録", signup_path %></li>
                <li class="navbar-text"><%= link_to "ログイン", login_path %></li>
            <% end %>
            </ul>
        </div>
    </nav>
</header>